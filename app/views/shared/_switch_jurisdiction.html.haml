.modal.hide.fade.embed-code{:id => "switch-#{response_set.id}"}
  .modal-header
    %button.close{:type => :button, 'data-dismiss' => :modal, 'aria-hidden' => true} &times;
    %h3= t('response_set.choose_a_jurisdiction')
  .modal-body
    %p= t 'response_set.dialog_body'
    -# only show surveys that aren't down for migration from
    - surveys = @surveys.keep_if {|s| ! Survey::MIGRATIONS.has_key? s.access_code}
    - surveys.sort! {|a,b| (a.full_title || a.title).to_s <=> (b.full_title || b.title).to_s }
    =# form_for [surveyor, response_set] do |f| (see surveyor#continue refactor note)
    = form_tag surveyor.continue_my_survey_path(survey_code: response_set.survey.access_code, response_set_code: response_set.access_code), method: :get, :class => 'form-inline' do
      = select_tag(:juristiction_access_code, options_for_select([[t('response_set.choose_jurisdiction'), ""]] + surveys.map{|s|[s.full_title || s.title, s.access_code]}))
      = submit_tag t('response_set.start_new_jurisdiction'), :class => 'btn btn-primary btn-submit'
