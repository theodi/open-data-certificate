.modal.hide.fade.embed-code{:id => "switch-language-#{response_set.id}"}
  .modal-header
    %button.close{:type => :button, 'data-dismiss' => :modal, 'aria-hidden' => true} &times;
    %h3= t('response_set.choose_a_language')
  .modal-body
    %p= t 'response_set.language_dialog_body'
    = form_tag surveyor.continue_my_survey_path(survey_code: response_set.survey.access_code, response_set_code: response_set.access_code), method: :get, :class => 'form-inline' do
      = select_tag :survey_locale, options_from_collection_for_select(@response_set.survey.translations.select(:locale), "locale", "locale", @response_set.locale), prompt: t('response_set.choose_language')
      = submit_tag t('response_set.start_new_language'), :class => 'btn btn-primary btn-submit'