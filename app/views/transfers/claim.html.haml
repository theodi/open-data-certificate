- content_for :header_title, t('.title')

%h3
  = t('.body', dataset_title: @transfer.dataset.title )
  

= form_for @transfer, url: {action: :accept}  do |f|
  = hidden_field_tag :token, params[:token]
  = f.submit t(".accept"), class: 'btn btn-primary'
