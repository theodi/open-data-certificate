- content_for :header_title do
  - if @back
    #{link_to @title, @back} -
  = t('.discussion')

-if @topic_help
  .discussion_help
    = markdown(@topic_help)

%p.join-discussion
  %a.btn.btn-primary.btn-large
    = t('.join_the_discussion')

= juvia_comments @topic

%hr
%p= link_to t(".markdown_help"), markdown_help_path