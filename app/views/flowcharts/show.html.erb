<!DOCTYPE html>
<!--[if lte IE 8]>     <html class="lt9 no-js oldie"> <![endif]-->
<!--[if gt IE 8]><!-->
<html class='no-js'>
  <!--<![endif]-->
  <head>
    <script src="/assets/mermaid.min.js"></script>
    <script>
      mermaid.initialize({startOnLoad:true});
    </script>
    <link href="/assets/mermaid.css" media="all" rel="stylesheet" type="text/css" />
  </head>
  <body>

  <form action="" method="get">
    <p>
        <label>Select jurisdiction</label>
        <%= select_tag(:jurisdiction, jurisdiction_options_for_select(@jurisdiction)) %>
        <%= submit_tag 'Submit', :class => 'btn btn-primary btn-submit' %>
    </p>
  </form>

  <div class="mermaid">
    graph TD
    <% @questions.each_with_index do |q, index| %>
      <% next if @questions[index + 1].nil? %>
      <%= render partial: "question.txt", locals: { question: q, index: index } %>
    <% end %>
    <%= @deps.reverse.join("\r\n").gsub("’", "'") %>
  </div>

</body>
</html>
