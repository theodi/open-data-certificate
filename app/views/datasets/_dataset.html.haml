%dl
  %dt
    = dataset.title
    = render dataset.transfer || dataset.build_transfer
  %dd
    = render partial: 'response_set', collection: dataset.response_sets, locals: {primary: dataset.response_sets.first}
  %hr.heavy
