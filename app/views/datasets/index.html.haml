- content_for :header_title, t('dashboard.heading__my_certificates')

// %p.lead= t('dashboard.heading__intro_text')

- if current_user.datasets.any?
  = render partial: 'datasets/dataset', collection: current_user.datasets
- else
  %p You don't have any certificates yet.
  = form_tag non_authenticated_start_questionnaire_path do
    %button.btn.btn-primary Start a new certificate
